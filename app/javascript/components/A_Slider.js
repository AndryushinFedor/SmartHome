import React from "react"
import PropTypes from "prop-types"
class A_Slider extends React.Component {
  render () {
    return (
      <React.Fragment>
      </React.Fragment>
    );
  }
}

export default A_Slider
